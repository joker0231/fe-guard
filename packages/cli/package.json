{
  "name": "@frontend-guard/cli",
  "version": "0.1.0",
  "description": "CLI tool for Frontend Guard - init, check, report",
  "bin": {
    "frontend-guard": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "glob": "^10.0.0"
  },
  "keywords": ["cli", "frontend-guard", "eslint", "code-quality"]
}
